{
  "name": "discord_re-envisioned",
  "version": "1.0.0",
  "description": "A discord client mod to allow the user to add plugins and themes to discord",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node ./installation.js --install",
    "uninstall": "node ./installation.js --uninstall",
    "update": "npm i && git stache && git pull"
  },
  "author": "Doggybootsy",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.44.0"
  },
  "devDependencies": {
    "electron-devtools-installer": "^3.2.0"
  },
  "info": {
    "name": "Discord Re-envisioned",
    "nameShort": "DrDiscord",
    "version": "1.0.2"
  }
}
